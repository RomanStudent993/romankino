# Кинопоиск - Django проект

Сайт для управления коллекцией фильмов, похожий на Кинопоиск.

## Возможности

- 📽️ Просмотр списка фильмов
- 🔍 Поиск фильмов по названию
- 🏷️ Фильтрация по жанрам
- 📄 Детальные страницы фильмов
- 👤 Красивая админ-панель с django-jazzmin
- 🖼️ Загрузка постеров фильмов
- 🎨 Сине-темносиняя цветовая гамма

## Установка и запуск

1. Убедитесь, что виртуальное окружение активировано:
   ```bash
   venv\Scripts\activate
   ```

2. Примените миграции:
   ```bash
   python manage.py makemigrations
   python manage.py migrate
   ```

3. Создайте суперпользователя для доступа к админ-панели:
   ```bash
   python manage.py createsuperuser
   ```

4. Запустите сервер разработки:
   ```bash
   python manage.py runserver
   ```

5. Откройте в браузере:
   - Главная страница: http://127.0.0.1:8000/
   - Админ-панель: http://127.0.0.1:8000/admin/

## Добавление фильмов

1. Зайдите в админ-панель: http://127.0.0.1:8000/admin/
2. Войдите с учетными данными суперпользователя
3. Добавьте жанры в разделе "Жанры"
4. Добавьте режиссёров в разделе "Режиссёры"
5. Добавьте фильмы в разделе "Фильмы" и заполните все поля:
   - Название фильма
   - Описание
   - Жанры (можно выбрать несколько)
   - Режиссёр
   - Дата выхода
   - Постер (картинка)

## Структура проекта

- `movies/` - приложение Django для управления фильмами
- `templates/` - HTML шаблоны
- `media/` - загруженные изображения (создаётся автоматически)
- `kinopoisk_project/` - настройки проекта Django

## Развёртывание на Render.com

### Подготовка к развёртыванию

1. Убедитесь, что все изменения закоммичены в Git:
   ```bash
   git add .
   git commit -m "Подготовка к развёртыванию"
   ```

2. Загрузите код на GitHub:
   ```bash
   git push origin main
   ```

### Настройка на Render.com

1. Зайдите на [render.com](https://render.com) и создайте аккаунт
2. Нажмите "New +" → "Web Service"
3. Подключите ваш GitHub репозиторий
4. Настройте следующие параметры:
   - **Name**: kinopoisk-project (или любое другое имя)
   - **Environment**: Python 3
   - **Build Command**: `./build.sh`
   - **Start Command**: `gunicorn kinopoisk_project.wsgi:application`
   - **Plan**: Free (или выберите платный план)

5. Добавьте переменные окружения в разделе "Environment":
   - `SECRET_KEY`: сгенерируйте новый секретный ключ (можно использовать: `python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"`)
   - `DEBUG`: `False`
   - `ALLOWED_HOSTS`: `your-app-name.onrender.com` (замените на ваш URL от Render)

6. Создайте PostgreSQL базу данных:
   - Нажмите "New +" → "PostgreSQL"
   - Выберите Free план
   - После создания базы, Render автоматически добавит переменную `DATABASE_URL`
   - Подключите базу к вашему веб-сервису

7. Нажмите "Create Web Service" и дождитесь завершения развёртывания

### После развёртывания

1. Создайте суперпользователя через Render Shell:
   - Зайдите в ваш сервис на Render
   - Откройте раздел "Shell"
   - Выполните: `python manage.py createsuperuser`

2. Ваш сайт будет доступен по адресу: `https://your-app-name.onrender.com`

### Важные замечания

- Медиа файлы (постеры) будут храниться локально на сервере Render
- Для постоянного хранения медиа файлов рекомендуется использовать облачное хранилище (AWS S3, Cloudinary и т.д.)
- На бесплатном плане Render сервисы могут "засыпать" после периода неактивности

## Технологии

- Django 5.2.8
- django-jazzmin - красивая админ-панель
- SQLite - база данных (для разработки)
- PostgreSQL - база данных (для production на Render)
- Gunicorn - WSGI сервер для production
- WhiteNoise - для обслуживания статических файлов

# romankino
